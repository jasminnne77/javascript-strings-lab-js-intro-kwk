var greeting = "Hello, everybody!";

var specialGuest = "Neil deGrasse Tyson"

var greetSpecialGuest = "greetSpecialGuest" + specialGuest + "!";

var topic = "space";

var conversation = `Let\'s talk about ${topic}.`;

var writeGreetingToDocument = `something to do with document...`;

// Type your answer into the console without the quote marks and see if it works.

var customCelebration = 'Kaboomie! Hachewie, yeaaaaaaaah. Shwing.';

var consoleCelebration = (`console.log(customCelebration);`)';


